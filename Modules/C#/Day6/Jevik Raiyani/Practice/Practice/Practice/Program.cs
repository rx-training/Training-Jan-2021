using System;
using System.Collections;
using System.Collections.Generic;

namespace Practice
{

    class Program 
    {
        static void Main(string[] args)
        {   //Delegate
            //    Document doc = new Document();
            //    doc.Text = "Document text goes here";

            //    var blogposter = new BlogPoster();
            //    var blogDelegete = new Document.SendDoc(blogposter.PostToBlog);
            //    doc.ReportSendingResult(blogDelegete);

            //    var emailSender = new EmailSender();
            //    var emailDelegate = new Document.SendDoc(emailSender.SendEmail);
            //    doc.ReportSendingResult(emailDelegate);



            //event
            //lamda
            //var theClock = new Clock();
            //var visibleClock = new VisibleClock();
            //visibleClock.Subscribe(theClock);
            //var logger = new Logger();  
            //logger.Subscribe(theClock);
            //theClock.RunClock();

            //func int
            //Func<int,int> squre = (x) => x * x;
            //Console.WriteLine(squre(Convert.ToInt32(Console.ReadLine())));


            

        }
    }
}
