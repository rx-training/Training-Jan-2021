function login(){
  var loginphonenumber = document.getElementById('mobilenologin').value;
  var loginpassword = document.getElementById('passwords').value;
  var loginpassword = document.getElementById('passwords').value;
  var loginpassword = document.getElementById('passwords').value;
  var loginpassword = document.getElementById('passwords').value;
  var loginpassword = document.getElementById('passwords').value;
  
  //alert(loginpassword+loginphonenumber);
  //getText("./Json/admindata.json");
  // async function getText(file){ 
  //    var n = await fetch(file);
  //    var object = await n.json();
  //    for(i=0;i<object.Admin.length;i++){
  //       if(object.Admin[i].phoneno == loginphonenumber && object.Admin[i].password == loginpassword)
  //       {
  //         location.href = "https://www.w3schools.com";
  //       } 
  //     }
  //     alert("Enter valid data");   
  //    }

}