import { GradeColorsDirective } from './grade-colors.directive';

describe('GradeColorsDirective', () => {
  it('should create an instance', () => {
    const directive = new GradeColorsDirective();
    expect(directive).toBeTruthy();
  });
});
